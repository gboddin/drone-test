pipeline:
  test-drone-plugin:
    image: fpfis/drone-plugin-test
    one_setting: test
    secret_forward: ${MY_SECRET}
    another_setting: test
    secrets: 
      - source: my_secret
        target: my_not_secret
      - source: my_secret
        target: my_super_secret
